; Allow native packages to run install scripts on Vercel (pnpm v10+)
; argon2 needs to compile native bindings
; prisma needs to download the query engine
; esbuild needs to download platform binaries
; unrs-resolver is a transitive dep that needs native compilation
auto-install-peers=true
